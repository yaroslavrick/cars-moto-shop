<h1><%= 'Welcome to Car Advertisements Web!' %></h1>
<%= button_to "Help", help_index_path,  {:method => :get, :class => "btn btn-primary"}  %>
<%= button_to "Cars List", cars_path,  {:method => :get, :class => "btn btn-primary"}  %>

<button>Search</button>

<!--if isn’t logged in-->
<button>Login</button>
<!--if isn’t logged in-->
<button>Sign up</button>
<!--if is logged in-->
<button>My searches</button>
<!--if is logged in-->
<button>Logout</button>
