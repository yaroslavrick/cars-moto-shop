<nav class="navbar navbar-expand-lg bg-body-tertiary bg-tertiary sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= "/" %>">CAR ADVERTISEMENTS WEB</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <li class="nav-item">
          <a class="nav-link" href="<%= root_path %>">
            <%= t('.home') %>
          </a>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link" href="<%= help_index_path %>">
            <%= t('.help') %>
          </a>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link" href="<%= cars_path %>">
            <%= t('.cars_list') %>
          </a>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link" href="<%= search_index_path %>">
            <%= t('.search') %>
          </a>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link" href="#">
            <%= t('.my_searches') %>
          </a>
        </li>
        
        <%- if user_signed_in? %>

          <li class="nav-item dropdown">
            <%= button_to t('.logout'), destroy_user_session_path, method: :delete, form: {turbolink: false}, class: 'nav-link bg-transparent border-0 shadow-none' %>
          </li>
        <% else %>
          <li class="nav-item dropdown">
            <a class="nav-link" href="<%= new_user_session_path %>">
              <%= t('.login') %>
            </a>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link" href="<%= new_user_registration_path %>">
              <%= t('.sign_up') %>
            </a>
          </li>

        <% end %>
        <%= render partial: 'shared/translation_drop_menu' %>
      </ul>
    </div>
  </div>
</nav>
